#!/bin/bash
echo "MOCK_ZIP: $*" >> "${MB_TEST_DIR:-/tmp}/mock_calls.log"

output=""
for arg in "$@"; do
    case "$arg" in
        -*) continue ;;
        *) output="$arg"; break ;;
    esac
done
[ -n "$output" ] && touch "$output" 2>/dev/null || true
exit 0
