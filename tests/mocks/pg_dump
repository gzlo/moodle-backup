#!/bin/bash
echo "MOCK_PG_DUMP: $*" >> "${MB_TEST_DIR:-/tmp}/mock_calls.log"
case "$1" in
    --version|-V) echo "mock-pg_dump 1.0" ;;
    *) echo "-- Mock PostgreSQL dump"; echo "CREATE TABLE test (id SERIAL PRIMARY KEY);" ;;
esac
exit 0
