#!/bin/bash
# Mock for mysqldump - records calls and returns success
echo "MOCK_MYSQLDUMP: $*" >> "${MB_TEST_DIR:-/tmp}/mock_calls.log"
echo "-- Mock database dump"
echo "CREATE TABLE test (id int);"
exit 0
